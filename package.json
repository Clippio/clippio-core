{
  "name": "clippio-core",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "The verifiable core of Clippio: payout math, vault accounting and the escrow specification, with the test suite that pins them.",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@solana/spl-token": "^0.4.9",
    "@solana/web3.js": "^1.95.0"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4"
  }
}
